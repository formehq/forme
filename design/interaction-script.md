# Forme 交互稿(5 屏)

> **状态:v0(agent 供料)→ Zayn 语气/形态定稿中 → 认可即 issue #1 验收 → W3 console 的设计输入。**
> **编辑守则**:事实与数字勿改(全部经核验,出处在 [issue #1 素材包](https://github.com/formehq/forme/issues/1));语气、人称、字段顺序、措辞、手势文案**任改**——直接改本文件 commit 即可。这是一份活文档:W3 console 动工前,用 `design-notes.md` 里攒的真实使用摩擦修订为 v1。
> 卡面里 Forme 对用户说话的那个声音,就是在这份文件里被定下来的。

## 4 个形态决定(边过屏边勾)

- [ ] **手势形态**:a/p/r 键盘键、点击,还是两者?correction 输入摆哪里?
- [ ] **信息层级**:先证据还是先 diff?(v0 = 标题 → 证据 → diff → 手势,可换)
- [ ] **语气人称**:v0 用了"平静同事腔、零催促、第一人称我/你"——对不对味?
- [ ] **出现时机**:"工作流边界"具体是哪几个时刻?(开盖?周会前?写完东西后?)

---

## 屏 1 · 开盖:catch-up 卡(agent 的 restart briefing)

场景:合盖一晚,早上开盖。第一屏永远是它,≤10 秒出现(硬约束 3)。

> **你不在的 14 小时**
> 进来:0 条新输入(Flomo 上次同步 06-29;Clippings 空)
> 我做的:事实核验 + 指标刷新;没动任何内容文件
> 等你:**3 张卡,约 2 分钟** · 距 07-14 硬锚(Validation v2 定稿 + R 判读)还有 10 天
>
> `[进入落子]  [先看 State Diff]  [今天不看]`

**设计意图**:`[今天不看]` 必须存在且零愧疚感——不催促是"识相"的底线(硬约束 2)。"我做的"一行永远交代 agent 在缺席期间动没动东西(信任的日常维护)。

---

## 屏 2 · 落子 session:3 张真卡(2 分钟)

场景:用户从 catch-up 卡点进来,或在工作流边界主动打开。成批出现,数量受接受率节流。

### 卡 1 — Zara 双笔记归位(漂移种类:文件在错误的成熟度层)

> **两条 Zara Zhang 材料在 Inbox 躺了 19 天,且已被正文引用**
> 证据:《社媒增长方案 - 2026-06-14》已引用其中一条(增长方法论一节),链接指向 `00_Inbox/` 的未归位 clipping;两文件 06-15 入库
> 最小 diff:移入 `01_Raw/Articles/` + 补 raw frontmatter;wikilink 按文件名解析、自动保持;Source Index 各加一行
>
> `[a]ccept   [p]ark   [r]eject   输入修正…`

### 卡 2 — 睡眠三连 → Energy-Aware Scheduling 接线(漂移种类:新料未接进 hub)

> **3 条睡眠 Flomo 未接进 Energy-Aware Scheduling**
> 证据:「睡眠 Principles - 固定起床锚定节律」「睡前重新学习慢下来」「解决睡觉问题 - 太久没有慢下来」(06-25 写,06-29 入库)直接命中 hub 的睡眠维度;该 hub 是 Home 的 Current Core Area,却自 06-03 零更新、对三条零引用
> 最小 diff:hub 增「睡眠实践(2026-06)」小节,三行引用 + 各一句定位;不改写原文
>
> `[a]ccept   [p]ark   [r]eject   输入修正…`

### 卡 3 — Pattern Map 判定(漂移种类:导航面在推荐一个停摆的东西)

> **Pattern Map 自 05-31 无实质更新(34 天),你称它"静态残骸",Home 仍在 Key Maps 里推荐它**
> 证据:你在《目前Vault的四点观察》的原话——"pattern reflecting = Pattern Map,但它是 05-31 后停更的静态残骸,没有交互模型"
> 最小 diff:Home 的 Key Maps 移除该行;文件标注 `status: dormant` +「待 Forme pattern-reflecting 原语接替」。**不归档、不删除**
>
> `[a]ccept   [p]ark   [r]eject   输入修正…`

**设计意图**:三张卡覆盖三种漂移、预期引出三种不同手势——落子 session 的多样性不是摆拍。每张卡自带出身(它为什么出现),provenance 可点开。

---

## 屏 3 · 周日 State Diff(一屏叙事,读完 ≈ 90 秒)

场景:周更 + 唤醒时增量;只读,没有任何要求动作的元素。

> **本周(06-28 → 07-04)**
>
> **进来了什么**:7 条 Flomo(06-29 批:睡眠三连、建筑师性训练、Studio 定位等);2 条 Zara clippings 仍在 Inbox(→ 卡 1)
>
> **变了什么**:CCS 定名 **Forme**(07-02)→ formehq.dev + GitHub org 注册(07-03)→ repo 建立,private(07-03)→ W0 提前 5 天收敛,W1 开(07-04)。Chaostudio 获得母题脊(Architect-ness 接线,07-01)。
>
> **什么在等你决定**:中文名 · 第一个非自己用户 · 八月行程 · @formehq · 3 张卡
>
> **警报**:Reports:Posts = 78:4(meta-work canary 红)——本周产物已转向 repo,下周此行应当变化;R 实验 6/10 条,07-14 判读

**设计意图**:四段固定结构(进来/变了/等你/警报)= State Diff 原语的可识别外观;内容生成式,骨架永远不变。

---

## 屏 4 · Taste 面板:规则被学到的时刻(demo 高潮)

场景:第 ~20 条决策触发规则提炼后,面板顶部出现一条待确认的新规则。

Demo 台词(非虚构):**这 7 条规则今天真实存在**——过去六周人肉回路里被 correction 出来、现写在 owner 的 CLAUDE.md 和 agent memory 里。产品做的只是把"提炼"这一环自动化。

> **Taste Rules(当前生效 7 条 · 人可编辑)**
> 1. 报告与总结用中文;路径、属性名、英文概念题名不动
> 2. 删除 / 合并 / 归档 / 发布必须人批(保守默认)
> 3. 私密与关系类材料不做转化,停在 Inbox 等本人
> 4. 自我呈现文案默认 honest-humble,主动砍浮夸
> 5. 写回 = 填真 gap,不复读已有内容;单一事实源
> 6. 先核验事实,再写回;只 stage 任务相关文件
> 7. 开发期不新增 vault 文档;产物优先代码 / demo
>
> ⚡ **新规则待确认(第 20 条决策触发提炼)**
> 「AI 起草的对外文案一律标注'脚手架、非终稿',发布前须经你自己的 voice 重写」
> 依据:Zara 修正(06-16)、首帖发布台、CV 打磨——三次同方向 correction
>
> `[收录]   [改写]   [丢弃]`

**设计意图**:**规则被学到本身也是一张 Decision 卡**——学习过程对人透明、可否决,trust boundary 的界面化。

---

## 屏 5 · 影子授权卡(agency 换挡的确认交互)

场景:某类操作用户从未拒绝,影子模式一致率达标后,agent 申请该类别的正式授权。

> **申请授权:「Flomo intake 归位」类**
> 记录:自 05-21 起,此类操作你 **11 次全部 accept、0 次回滚**(git 历史可查)
> 影子一致率:`[产品将真实计算,门槛 >95%]`(样稿不造数)
> 授权范围:仅「移动文件 + 补 frontmatter + 索引加行」;任何内容改写仍然出卡
>
> `[授权此类]   [继续影子]   [收紧到逐条]`

**设计意图**:授权按**类别**给、范围写死、随时可收紧。这张卡同时回答了 spec 的 Open Decision「影子→正式授权的确认交互形态」:就是一张 Decision 卡。
