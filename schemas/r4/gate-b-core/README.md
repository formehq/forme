# R4 Gate B Demo-critical Core

- Status: `PHYSICAL_ADAPTERS_CONSTRUCTED_OFFLINE_RETRY_NOT_REQUESTED`
- Physical Construction Packet:
  `sha256:7ad7fd34d618b03b0cafffbe1b65c9516e0bd3bdcc0e329408f1d85e38669d06`
- Host-Binding / Adapter Construction Grant: `APPROVED`
- Retry Execution Grant: `NOT_REQUESTED`
- First Provider-Call Test Grant: `NOT_REQUESTED`

This directory remains the separate Demo-critical Core overlay: exactly 32
active operations for the public Hero Encounter, with the 13 Full-only
operations absent from active API, Web and CLI.

The repository now contains closed, typed future physical adapters for all
three Gate B lanes:

- PostgreSQL: 13 named race families expanded into 16 executable cases and 32
  fresh-overlay commit orders, with exactly 68 planned Core calls and 32
  persisted-state verifiers;
- Codex: four exact host-capsule-bound `sandbox-exec` process shapes, bounded
  process-group cleanup, schema validation and the explicit
  `UNPROVEN_ACCEPTED` post-response causal limit;
- macOS: direct compile/bundle/sign/custom-Keychain/LA/helper execution plan,
  exact feeder-to-helper pipe commit gate, terminal-specific receipt evidence
  and cleanup after every known step.

All of those mechanisms were constructed and tested through fake executors or
normal compiler/unit-test subprocesses. This state does **not** prove that
Docker/PostgreSQL, real Codex, Seatbelt, codesign, Keychain, LocalAuthentication
or the signed helper ran. It also does not open a provider, Guest, publication
or deployment path. A future Retry requires an exact, separately approved
Manifest and a still-valid private Host Binding Capsule.

The authoritative map is [`runtime-boundary.json`](./runtime-boundary.json).
The machine inventory is [`artifact-index.json`](./artifact-index.json).
Historical Core construction evidence schemas remain immutable; successor
construction and Retry evidence use the distinct `physical-*.schema.json`
roots in this directory.
