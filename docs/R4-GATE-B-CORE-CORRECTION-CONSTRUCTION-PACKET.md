# R4 Gate B Core Correction Construction Packet v0.1

- Status: **approval candidate; `PROPOSED_NOT_EXECUTED`**
- Updated: 2026-08-07
- Parent authority: R4 Technical Control Packet v0.2
- Parent Packet SHA-256:
  `sha256:e417836bd67bdef73f401919e83de3d58f68960499bd5c356951b48408adfff5`
- Original Gate B Manifest SHA-256:
  `sha256:ba0f9ce389c6668aef5e41fb2e6228d8838d7bea481f942b2a025620d37ad5fa`
- Retry Construction Packet SHA-256:
  `sha256:4122e293fb476dc90e289566745459d9fe1b9603c3473c49de9d2e1429e025e7`
- Owner-approved Correction Scope Decision Brief SHA-256:
  `sha256:c20e987cfb7ff7cc2b73c1d13584a8d7955bd5c3407369bed3a98ce37700f86f`
- Correction Construction Grant: **REQUESTED BY THIS PACKET**
- Retry Execution Grant: **NOT REQUESTED**
- First Provider-Call Test Grant: **NOT REQUESTED**
- Provider sessions / bytes / spend authorized here: **0 / 0 / US$0**

This Packet translates the Owner-approved five-part scope correction into one
exact repository-only Construction boundary. It requests no physical retry.
Approval would permit only the files, fake adapters, offline tests and
body-free proposal artifacts named below.

The approved Scope Decision Brief remains byte-frozen even though its own
header says that an Owner decision was required. The external Owner receipt is:

```text
批准 R4 Gate B Correction Scope Decision Brief 五项推荐；只授权准备 exact Correction Packet；Correction Construction、Retry Execution 与 First Provider-Call Test Grant 均 NOT REQUESTED
```

This Packet intentionally does not contain its own SHA-256 or containing Git
commit. A separate low-load Owner Review binds those values after the bytes
freeze.

## 1. Outcome and claim amendment

Construction should make the next physical proposal capable of proving one
truthful Demo-critical Core:

```text
fixed synthetic Gate B Controller / Curator / Third Place / Entity basis
  → public Room creation and exact local pairing
  → Projection delivery and Curator admission
  → public Third Place read and one bounded knock
  → local sync, pull, ACK and Owner review
  → Fresh-cycle reservation with zero provider dispatch in Retry
  → transient synthetic candidate review mechanism
  → exact Response delivery and one bounded GrantOffer continuation
  → revoke / terminal unreadability / idempotent recovery
```

This sequence names the Core capabilities, not one body-bearing end-to-end
bridge in the zero-provider Retry. PostgreSQL Response delivery uses a
synthetic fixture; the native helper only count/discards its synthetic
candidate in-process. Connecting candidate bytes to publication remains behind
the First Provider-Call construction/grant and later Gate C activation.

This is an explicit Core overlay, not a rewrite of the Full target. The exact
Technical Control Packet v0.2, original Full-45 API, 53 SQL surface functions,
27-category database suite, persistent candidate contract and historical
Gate B evidence remain immutable and incomplete.

The active claims are:

- `CORE_CONSTRUCTED_OFFLINE` may be used only after this Packet's repository,
  fake-executor and static tests pass;
- `POSTGRES_CORE_LANE_GREEN` may be used only after a later separately
  approved physical execution passes Core-32 semantics, races, rollback and
  cleanup on the pinned disposable PostgreSQL;
- `GREEN_TRANSIENT_MACOS_MECHANISM_ONLY` may be used only after a later
  separately approved signed-helper run passes on one pinned host;
- a clean real Codex zero-call diagnostic may report
  `ZERO_CALL_CHILD_MECHANISM_GREEN`, while `CODEX_AI_LANE_YELLOW` and
  `AGGREGATE_RETRY_YELLOW` remain mandatory until a separately approved First
  Provider-Call Test passes; and
- `FULL_GATE_B_GREEN` remains unavailable until the original Full contract is
  actually completed.

Core does not physically prove a Private Room, notification email, Agent
derivative, Direct Invite, Room retirement, persistent candidate, production
identity, Web-to-PostgreSQL deployment bridge or real Guest encounter. The
existing versioned static Projection remains the schedule fallback and may not
be presented as a Fresh Session result.

The approved D3 phrase “locked memory” is made exact here: every
Forme-helper-owned candidate-body buffer must be successfully `mlock`ed before
the first candidate byte is read. Failure is Yellow/manual-only with zero read,
display and handoff. Framework-managed UI copies, swap behavior and crash-time
byte-for-byte erasure are outside that claim; controlled terminal paths must
still zeroize every Forme-owned buffer. This is a claim clarification the
Owner approves together with this Packet, not a silent weakening of D3.

## 2. Exact authority ceiling

Approval of this Packet authorizes only:

1. one primary Agent, working serially in the Forme repository, to modify or
   create only the exact workset in Section 6;
2. a separate Core-32 operation map, generated schemas and inactive Full
   compatibility regression without changing the frozen Full artifacts;
3. static Core PostgreSQL SQL plus an injected fake Docker/SQL executor;
4. a no-search Codex staging/process/profile adapter tested only with a
   synthetic package, fake Codex executable and fake sandbox wrapper;
5. a transient-candidate native source path, unsigned Swift compile/unit tests
   and an in-process fake macOS effect executor;
6. Core Web/API/CLI exposure changes that hide every excluded operation before
   body or authentication processing;
7. construction of one aggregate runner in dry-run/fake mode, a
   fault-journaled marker contract and live-parent cleanup plan;
8. Gate A and historical Full regression, TypeScript checking, Swift tests,
   offline repository tests and static effect audits;
9. a body-free Core Construction Report, machine evidence, a corrected exact
   Execution Manifest and its low-load Owner Review; and
10. intentional commit, push, Draft PR and GitHub issue/project maintenance
    after local validation, without merge.

Approval does **not** authorize:

- Docker, image pull, container, volume, PostgreSQL process or SQL execution;
- reading or enumerating a real Codex installation/home, or invoking real
  Codex, `sandbox-exec`, a Codex thread, turn, model, account or provider;
- assembling, signing, launching or assessing an app; creating a certificate,
  private key or Keychain; calling LocalAuthentication, Secure Enclave or any
  real user-presence UI;
- a real Guest, request, source snapshot, candidate, Room credential, email,
  external message, hosted or production mutation;
- a new dependency, package-lock change, network test, deployment, Gate C,
  public traffic, merge or spend.

The normal compiler/test process tree is permitted. Fake Docker and macOS
effect executors are purely in-process: they may not spawn a process, open a
socket, call a Security/LocalAuthentication API or escape the isolated
Construction temp root. The Codex process-supervisor fixture is the only
exception: each of its four steps may start one exact fake sandbox wrapper
which `exec`-replaces itself with one exact fake Codex executable; a designated
fork/orphan adversarial case may start at most one additional synthetic
descendant. Every such executable lives beneath the Construction temp root,
opens no socket/network path, is counted, and is reaped before its test
returns. The only network actions permitted are the final already-authorized
Git/GitHub publish and project-state updates after Construction succeeds.

Any unknown capability, unlisted file, unlisted external read, dependency drift, real
runtime invocation, sensitive evidence or cleanup ambiguity is Red and stops
Construction.

### 2.1 Development-carrier accounting

The existing Owner-visible Codex Desktop task is the engineering carrier. The
zero provider/session ceiling means Forme code or tests may start no additional
real model/Codex session. It does not claim that the already-visible human
development conversation uses no provider infrastructure.

No subagent may implement, execute, investigate or clean a Construction lane.
Independent review after the workset is frozen may inspect repository bytes
only and may not invoke a physical adapter.

## 3. Baseline and immutable lineage

| Binding | Exact value |
|---|---|
| Branch | `codex/r4-gate-a-build` |
| Code baseline commit | `097ee44abc4db40e63cc4d9382a25e50f5a88bab` |
| Code baseline tree | `92c0d067a326ef264563df89b529879573d46ac0` |
| Draft PR | `#65` |
| `package-lock.json` | `sha256:d7a56f2e40ffc80f03413c8e697e1a9a9199dcb8873cedc43cd421a2b265c812` |
| Retry Construction Report | `sha256:457e1d2257623fd1332d448a1381cde5b658dd89cad620096a66f44f05416b96` |
| Retry machine evidence | `sha256:0d0053c761ae292de0706b02b692c719fcb18ecf23c2664bb040a5ca9d345e71` |
| Non-approvable Retry Execution Manifest | `sha256:f2276d7dfd5716bc5f14e7853925cbc991349b525eb28e0453c9cf9b4602b976` |

The later Owner Review records the Packet-containing proposal commit and tree.
Construction preflight must require that exact approved commit, a clean
worktree and no upstream divergence. The code baseline above remains the
before-state for all implementation bytes.

These approval/evidence objects are immutable:

```text
docs/R4-TECHNICAL-CONTROL-PACKET.md
docs/R4-GATE-B-SCHEMA-RUNTIME-MIGRATION-MANIFEST.md
docs/R4-GATE-B-OWNER-REVIEW.md
docs/R4-GATE-B-EXECUTION-REPORT.md
docs/evidence/r4-gate-b-execution.json
docs/R4-GATE-B-RETRY-CONSTRUCTION-PACKET.md
docs/R4-GATE-B-RETRY-OWNER-REVIEW.md
docs/R4-GATE-B-RETRY-CONSTRUCTION-REPORT.md
docs/evidence/r4-gate-b-retry-construction.json
docs/R4-GATE-B-RETRY-EXECUTION-MANIFEST.md
docs/R4-GATE-B-RETRY-EXECUTION-OWNER-REVIEW.md
docs/R4-GATE-B-CORRECTION-SCOPE-DECISION-BRIEF.md
docs/R4-GATE-B-CORE-CORRECTION-CONSTRUCTION-PACKET.md
docs/R4-GATE-B-CORE-CORRECTION-CONSTRUCTION-OWNER-REVIEW.md
```

The last two files are proposal artifacts: they exist before Construction and
are not Construction outputs. Once the Owner approves their exact SHA-256
values, both become byte-immutable. Construction preflight must bind both
approved hashes plus the proposal commit/tree published for review; neither
file may be edited to record approval or implementation results.

All existing paths beneath these roots are also immutable Full/historical
inputs during Construction:

```text
schemas/r4/gate-b/
schemas/r4/sql/
fixtures/r4-gate-b/
schemas/r4/api-v1.schema.json
schemas/r4/api-v1-index.json
schemas/r4/local-formats.schema.json
schemas/r4/local-formats-index.json
schemas/r4/gate-b-evidence.schema.json
native/macos/Sources/FormeLocal/KeychainProtector.swift
native/macos/Sources/FormeLocal/GateBProbe.swift
packages/r4-local/src/candidate-store.ts
```

In particular, the Full `forme-fresh-response.sb`, Full macOS build recipe,
original runtime boundary and persistent candidate implementation are not
edited to make Core appear complete.

## 4. PostgreSQL Core-32 surface

Core has exactly **32 HTTP/API operations: 6 reads, 26 mutations and 21
mutations requiring `If-Match`**. Unless narrowed below, their method, path,
actor, closed request/result objects, idempotency and error semantics are
inherited byte-for-byte from the frozen Full operation map.

### 4.1 Exact included operations

Public / Guest — 8:

```text
third_place.list
projection.read
public_encounter.issue
interaction.create
interaction.read
interaction.delete
grant_offer.accept
room.pair.exchange
```

Controller — 11:

```text
control.status
control.interaction.read
room.create
room.pair
room.binding.revoke
projection.revoke
response.revoke
grant.revoke
grant_offer.issue
grant_offer.revoke
interaction.close
```

Curator — 2:

```text
curation.admit
curation.unlist
```

Room Operator — 11:

```text
room_operator.status
room_operator.sync
room_operator.pull
room_operator.cycle.reserve
room_operator.cycle.recover
room_operator.cycle.abandon
room_operator.dispatch.issue
room_operator.ack
room_operator.projection.deliver
room_operator.response.deliver
room_operator.local_purge.receipt
```

Core additionally narrows the inherited contracts:

- `room.create` accepts only `third_place_public`;
- `projection.read` serves only the current Core public Projection;
- `interaction.create` accepts only PublicEncounter or accepted Grant
  authority, never AgentDerivative;
- GrantOffer targets only the current public Room and Projection;
- all four already-approved Grant presets remain available, but replacement
  is absent; and
- every actor, Room, Projection, Interaction, Grant and binding remains exact
  Room-scoped and synthetic in Gate B.

### 4.2 Exact excluded operations

These 13 Full operations are not registered in the active Core API, Web or CLI:

```text
notification.set
notification.remove
notification.verify
direct_invite.redeem
agent_derivative.mint
room.mode.set
room.retire
room.delete
grant.issue
grant.replace
direct_invite.issue
direct_invite.revoke
room_operator.stale.attest
```

Every excluded method/path returns the same body-free `404 not_found` before
query/body parsing, content-type checks, Bearer/header processing, database
access or feature-state disclosure. It may not return `503`, an experimental
flag, a disabled control, or a different error that confirms the route.

Full `OPERATION_INVENTORY` may remain for immutable history/regression, but
`matchCoreOperation()` is the only matcher used by the active HTTP route.
Core schemas/indexes are bijective at `32 / 26 / 6 / 21`; Core HTTP, SQL and
CLI surfaces have exactly the same 32 actions.

`runCore()` and `ownerStatusCore()` enforce the same cut below routing: they
reject a Full-only action and also reject any resolved Private Room,
AgentDerivative, private binding, non-Core Projection or capability. The
existing direct Full application path remains available only to historical
regression tests. Active Owner pages and Projection pages call the Core path,
so filtering the route matcher cannot leave hidden Full data visible.

Web removes the Private Room entry and notification controls from the active
Core experience. The grant-offer continuation remains. A direct Private Room
URL fails closed before reading a Projection. The CLI removes the active
`forme guest agent-token` path and adds one allowlisted
`forme room api <core-operation>` command whose closed request arrives through
the secure input descriptor. Existing friendly Core commands may remain
aliases. The caller cannot choose a URL, method or header name and cannot send
untyped input. Only the action-specific path parameters, closed body and
secure capability fields defined by the Core index are accepted; all other
fields and actions fail before any HTTP request is built.

## 5. Synthetic basis and Core SQL contract

### 5.1 Setup-only basis function

Core adds exactly one non-product SQL operation:

```sql
forme_r4.tx_gate_b_core_basis_install(
  p_ctx forme_r4.mutation_context_v1,
  p_basis_hash forme_r4.sha256_digest
) RETURNS forme_r4.api_result_v1
```

It is `SECURITY DEFINER`, has the fixed `search_path = pg_catalog, forme_r4`,
is executable only by `forme_r4_migrate`, and has no HTTP, Web or CLI route.
The runner calls it inside one `SERIALIZABLE` transaction after Core v1 has
created the tables and function.

`schemas/r4/gate-b-core/core-basis.json` is exactly the following one UTF-8
line plus one terminal LF, with no BOM or additional whitespace:

```json
{"schemaVersion":"r4.gate-b-core-basis.v1","controllerSubjectId":"subject_gatebcorecontroller0001","controllerIssuerHash":"sha256:11be35a7bc87b2b2569b7615d9c15980a5be36934f7cd377634a6b752760fee2","controllerProviderSubjectHash":"sha256:877f7b87a8dbc2ae30bd6d0d7917f083cfd9f86b6cf45e1abada5c034d0f9eb6","controllerRoleAssignmentId":"role_gatebcorecontroller000001","curatorSubjectId":"subject_gatebcorecurator000001","curatorIssuerHash":"sha256:77c2c89119d707640c13182ebd59e0cd43acb08c48396b6638a66b69e7d9cf27","curatorProviderSubjectHash":"sha256:3c9e8546880091ba65e4f1b4c1463d3d4df308f7ee8189e8b61f15621ed2f1b0","curatorRoleAssignmentId":"role_gatebcorecurator00000001","thirdPlaceId":"thirdplace_gatebcore000000001","thirdPlaceSlug":"forme-demo","thirdPlacePublicLabel":"Forme Third Place","thirdPlaceCreatedEventId":"event_gatebcorethirdplace00001","entityId":"entity_gatebcoreforme00000001","entityPublicLabel":"Forme","idempotencyKey":"gate_b_core_basis_install_v1","actorScopeDigest":"sha256:b6c733e7298f287d559ee69afa6a0fdd6312248388c5db493ee3c80a2b2d5993","canonicalRequestHash":"sha256:a0b7bfe19e9583c68e715afcd361a06cd3ddb7dc57165efc77188e02ea12d309","correlationId":"8a7b8a0f-44d5-4c5e-a1b2-8b77b7e0c401"}
```

Its exact file hash is
`sha256:eabd968569b8245a7d6ed15493a3e79a59c913304e8d429169bf611b3d173d35`.
`p_basis_hash` must equal that value. `p_ctx` must equal the fixed Controller
subject, actor class `controller`, actor-scope digest, idempotency key,
canonical-request hash and correlation ID above, with
`expected_object_version = NULL`; any mismatch rejects before a row changes.

In one transaction the function creates exactly the two subjects, two roles,
one Third Place, its `created` event and one Forme Entity named above. The
Controller role has `granted_by_subject_id = NULL`; the Curator role is granted
by the fixed Controller. The Third Place event uses the fixed event ID and
canonical-request hash. Because `operation_receipts` and `idempotency_records`
are deliberately Room-scoped and no Room may be seeded, the setup function
does not forge rows in those tables. Its fixed event plus
the exact basis rows form the migrate-only idempotency witness: an exact
replay returns a body-free no-op; any partial presence, alternate key/hash or
row mismatch rejects. This preserves the 37-table product skeleton without a
hidden setup table.

The closed setup results are exact:

| Condition | Status / code | Target / version | Receipt | Closed result |
|---|---|---|---|---|
| fresh exact install | `201 gate_b_core_basis_installed` | `thirdplace_gatebcore000000001` / `1` | `NULL` | `{schemaVersion:"gate_b_core_basis_install_result.v1",eventId:"event_gatebcorethirdplace00001"}` |
| exact replay | `200 gate_b_core_basis_already_installed` | fixed Third Place / `1` | `NULL` | same result |
| basis hash, idempotency key or canonical-request hash differs | `409 gate_b_core_basis_request_conflict` | `NULL` | `NULL` | `{code:"gate_b_core_basis_request_conflict"}` |
| actor, scope, correlation or expected-version context differs | `403 gate_b_core_basis_context_denied` | `NULL` | `NULL` | `{code:"gate_b_core_basis_context_denied"}` |
| partial, altered or extraneous business state exists | `409 gate_b_core_basis_state_conflict` | `NULL` | `NULL` | `{code:"gate_b_core_basis_state_conflict"}` |

It refuses to run if any other business row exists. Room creation,
pairing/exchange, Projection delivery and Curator admission must then use their
formal Core functions; no test or runner may insert those rows directly.
Core `tx_room_create` additionally rejects unless the acting Controller,
`entity_id` and `home_third_place_id` are exactly the active basis values above
and `room_kind = third_place_public`.

This is the exact approved interpretation of “same Core function surface” for
synthetic Gate B setup. It avoids pretending that Gate B has production login,
identity/role provisioning, Third Place creation or Entity onboarding APIs.
Adding those product operations remains Gate C/post-demo work.

### 5.2 Exact SQL inventory

Core uses a separate migration set while preserving the Full files unchanged.
It retains the Full skeleton's exact 37 tables, 44 text domains, 3 composites,
11 audit views and these six invariant helpers:

```text
is_encrypted_field_v1
enforce_projection_room_entity
enforce_projection_immutability
enforce_response_expiry
enforce_terminal_unreadability
enforce_room_event_sequence
```

It implements the following exact action-to-SQL map:

| Core action | SQL function |
|---|---|
| `third_place.list` | `api_third_place_list` |
| `projection.read` | `api_projection_read` |
| `public_encounter.issue` | `tx_public_encounter_issue` |
| `interaction.create` | `tx_interaction_create` |
| `interaction.read` | `api_interaction_read` |
| `interaction.delete` | `tx_interaction_delete` |
| `grant_offer.accept` | `tx_grant_offer_accept` |
| `room.pair.exchange` | `tx_room_pair_exchange` |
| `control.status` | `api_control_status` |
| `control.interaction.read` | `api_control_interaction_read` |
| `room.create` | `tx_room_create` |
| `room.pair` | `tx_room_pair_issue` |
| `room.binding.revoke` | `tx_room_binding_revoke` |
| `projection.revoke` | `tx_projection_revoke` |
| `response.revoke` | `tx_response_revoke` |
| `grant.revoke` | `tx_grant_revoke` |
| `grant_offer.issue` | `tx_grant_offer_issue` |
| `grant_offer.revoke` | `tx_grant_offer_revoke` |
| `interaction.close` | `tx_interaction_close` |
| `curation.admit` | `tx_curation_admit` |
| `curation.unlist` | `tx_curation_unlist` |
| `room_operator.status` | `api_room_operator_status` |
| `room_operator.sync` | `tx_room_operator_sync` |
| `room_operator.pull` | `tx_room_operator_pull` |
| `room_operator.cycle.reserve` | `tx_fresh_cycle_reserve` |
| `room_operator.cycle.recover` | `tx_fresh_cycle_recover` |
| `room_operator.cycle.abandon` | `tx_fresh_cycle_abandon_zero_dispatch` |
| `room_operator.dispatch.issue` | `tx_dispatch_permit_issue` |
| `room_operator.ack` | `tx_room_event_ack` |
| `room_operator.projection.deliver` | `tx_projection_deliver` |
| `room_operator.response.deliver` | `tx_response_deliver` |
| `room_operator.local_purge.receipt` | `tx_local_purge_receipt` |

That is exactly 32 API functions plus the one setup-only function and the six
invariant helpers: **39 total functions**. No Core SQL body may call or
return `api_unavailable_result()`. The notification/janitor workers and every
Full-only API function are absent from the Core function inventory. Dormant
Full-only table skeletons are structural compatibility only and are never
claimed as implemented behavior.

Runtime roles receive no direct table authority and may execute only their
exact Core functions. Notification and janitor roles receive no Core semantic
function. The setup function remains migrate-only. A positive grant matrix and
global revoke prove the boundary.

The Core migration is atomic and ledgered against the Technical Packet,
approved Scope Brief, this Packet, Core basis and future Execution Manifest
hashes. It supports only disposable compatibility:

```text
empty disposable Core v0
  → bootstrap
  → Core v1 apply
  → basis install through setup-only function
  → verify
  → Core rollback to empty v0
  → bootstrap/Core v1 reapply
  → basis install
  → verify
```

Production rolling migration/restore is Gate C. No production driver is added
here. The later disposable lane uses `docker exec ... psql` through the closed
runner; Web-to-PostgreSQL deployment wiring remains a separately disclosed
Gate C requirement.

### 5.3 Exact real-race plan for later Execution

Construction builds fake plans and SQL barriers for these 13 race families;
the later separately approved Retry must run every family with concurrent
tracked `psql` workers and both commit orders where applicable:

1. pairing exchange with two candidate binding secrets;
2. two Interactions against the same one-use Encounter;
3. each frozen public-rate edge at its final slot: Room accepted pool
   `20/rolling-24h`, encounter issuance `10/bucket/Room/hour` and
   `50/bucket/Room/day`, and accepted submissions
   `3/bucket/Room/rolling-24h`;
4. GrantOffer accept versus Owner revoke;
5. accepted Grant at `quota` versus `quota+1`;
6. two distinct reservation IDs and idempotency keys concurrently call
   `tx_fresh_cycle_reserve` for one Interaction with the same start/envelope
   hashes: one returns `200`, the other `409 cycle_already_reserved`;
   subsequent exact recover of the winner returns `200`, while recover with
   the loser ID returns `409 cycle_recovery_mismatch`;
7. dispatch permit versus zero-dispatch abandon;
8. Response delivery versus Owner close;
9. Response delivery versus Guest delete;
10. Response delivery versus Projection revoke;
11. `tx_room_operator_pull` versus `tx_room_binding_revoke` for the same
    binding and Interaction: pull-first yields pull `200` then revoke `200`;
    revoke-first yields revoke `200` and pull `404 not_found`;
12. Curator unlist versus new public Encounter; and
13. same-key concurrent replay.

Every winner/loser result is controlled and idempotent. Race assertions and
persisted evidence are body-free; each SQL call's in-process result still
matches its inherited closed API-result schema and is discarded after
validation. No test accepts a static preset result as database evidence.

Room-event sequence monotonicity, exact event identity, ACK high-water
advancement, duplicate ACK no-op and out-of-order ACK rejection remain
required non-race semantic tests. They are not counted as a fourteenth race.

## 6. Exact repository workset

All tracked files not explicitly allowed below are read-only. New file creation
outside the exact list is Red and requires a new Packet.

### 6.1 New Core contract, SQL and fixture paths

```text
schemas/r4/gate-b-core/README.md
schemas/r4/gate-b-core/operations.md
schemas/r4/gate-b-core/api-v1.schema.json
schemas/r4/gate-b-core/api-v1-index.json
schemas/r4/gate-b-core/postgres-contract.md
schemas/r4/gate-b-core/core-basis.json
schemas/r4/gate-b-core/runtime-boundary.json
schemas/r4/gate-b-core/artifact-index.json
schemas/r4/gate-b-core/evidence.schema.json
schemas/r4/gate-b-core/codex-zero-call-contract.json
schemas/r4/gate-b-core/sql/0000_r4_gate_b_core_bootstrap.sql
schemas/r4/gate-b-core/sql/0001_r4_gate_b_core_presence.sql
schemas/r4/gate-b-core/sql/0001_r4_gate_b_core_presence.verify.sql
schemas/r4/gate-b-core/sql/0001_r4_gate_b_core_presence.rollback.sql
schemas/r4/gate-b-core/macos/transient-candidate-contract.json
schemas/r4/gate-b-core/macos/build-recipe.json
schemas/r4/gate-b-core/macos/forme-codex-zero-call.sb
schemas/r4/gate-b-core/macos/forme-core-transient-response.sb
schemas/r4/gate-b-core/macos/evidence.schema.json
fixtures/r4-gate-b-core/postgres/core-happy-path.sql
fixtures/r4-gate-b-core/postgres/core-errors.sql
fixtures/r4-gate-b-core/postgres/core-races.sql
fixtures/r4-gate-b-core/codex/initialize-result.json
```

### 6.2 New TypeScript/runner/test paths

```text
packages/r4-codex-adapter/src/zero-call-physical.ts
packages/r4-local/src/hosted-room-api.ts
apps/room/src/core-policy.ts
scripts/r4-gate-b-core-api-contract.mjs
scripts/r4-gate-b-core-postgres.mjs
scripts/r4-gate-b-macos-core.mjs
test/r4-gate-b-core/api-contract.test.ts
test/r4-gate-b-core/postgres-static.test.ts
test/r4-gate-b-core/postgres-adapter.test.ts
test/r4-gate-b-core/surface-parity.test.ts
test/r4-gate-b-core/web-cli-surface.test.ts
test/r4-gate-b-core/codex-physical-adapter.test.ts
test/r4-gate-b-core/macos-core-adapter.test.ts
```

### 6.3 New native paths

```text
native/macos/Sources/FormeCoreLocal/CoreLauncher.swift
native/macos/Sources/FormeCoreLocal/TransientCandidateSession.swift
native/macos/Sources/FormeCoreLocal/TransientCandidateReviewWindow.swift
native/macos/Sources/FormeCoreLocal/UserPresenceAuthorizer.swift
native/macos/Sources/FormeCoreLocal/CoreProcessSupervisor.swift
native/macos/Sources/FormeCoreLocal/CorePhysicalEvidence.swift
native/macos/Sources/FormeCoreLocal/CoreSandboxProfile.swift
native/macos/Sources/FormeCoreLocal/CoreLockedMemory.swift
native/macos/Sources/FormeCoreLocal/CountingHandoffPort.swift
native/macos/Resources/FormeCoreLocal.Info.plist
native/macos/Resources/FormeCoreLocal.entitlements
native/macos/Tests/FormeCoreLocalTests/TransientCandidateTests.swift
```

### 6.4 Construction output artifacts

```text
docs/evidence/r4-gate-b-core-construction.json
docs/R4-GATE-B-CORE-CONSTRUCTION-REPORT.md
docs/R4-GATE-B-CORE-EXECUTION-MANIFEST.md
docs/R4-GATE-B-CORE-EXECUTION-OWNER-REVIEW.md
```

Those paths do not exist before Construction. Their presence in this workset
does not create an Execution Grant.

### 6.5 Existing files allowed to change

```text
package.json
tsconfig.json
src/cli.ts
scripts/r4-doc-audit.mjs
scripts/r4-gate-b-preflight.mjs
scripts/r4-gate-b-runner.mjs
scripts/r4-gate-b-cleanup.mjs
scripts/r4-gate-b-codex-probe.mjs
packages/r4-codex-adapter/src/app-server-probe.ts
packages/r4-codex-adapter/src/index.ts
packages/r4-local/src/cli.ts
packages/r4-local/src/index.ts
apps/room/src/operation-inventory.ts
apps/room/src/http.ts
apps/room/src/application.ts
apps/room/src/projection-page.ts
apps/room/src/components/GuestStatus.tsx
apps/room/src/components/OwnerControls.tsx
apps/room/src/components/client-api.ts
apps/room/app/page.tsx
apps/room/app/layout.tsx
apps/room/app/owner/page.tsx
apps/room/app/owner/interactions/[interactionId]/page.tsx
apps/room/app/private/[projectionId]/page.tsx
native/macos/Package.swift
test/r4-gate-b/codex-adapter.test.ts
test/r4-gate-b/macos-boundary.test.ts
test/r4/hosted-http.test.ts
test/r4/hosted-web-agent-parity.test.ts
test/r4/ui-static-safety.test.ts
test/r4/local-cli.test.ts
```

These files may change only to wire the named Core surface/adapters/tests.
There is no permission to refactor unrelated Gate A behavior. No existing file
may be deleted. `package-lock.json` must remain byte-identical and no dependency
may be added or changed.

## 7. Construction execution contract

### 7.1 Exact serial order

```text
preflight: approved commit/tree, clean worktree, workset and toolchain
→ Gate A + historical Full regression
→ Core-32 operation/schema/index generation and audit
→ Core Web/API/CLI exposure and excluded-route proof
→ Core SQL/static contract + fake Docker/psql adapter
→ encrypted-field regression with synthetic canaries
→ zero-call diagnostic profile/staging/process adapter with fake children
→ transient-candidate Swift source + fake macOS physical plan
→ aggregate runner dry run and injected fault matrix
→ dependency/workset/body-free/static-effect audits
→ Core Construction evidence/report
→ corrected Execution Manifest + Owner Review
→ cleanup + STOP
```

No later lane runs after Red except cleanup. Yellow may continue only for an
independent lane and must disable the affected later capability. Each lane
writes one body-free checkpoint through the aggregate runner; the Agent does
not create ad-hoc checkpoints or investigate outside repository/fixture bytes.

### 7.2 Repository and environment boundary

Every command runs from the Forme repository. Reads are limited to the exact
approved proposal commit, the Section 6 workset and generated bytes beneath a
fresh Construction root. Every path component is checked with `lstat` or an
equivalent no-follow primitive and any symlink is rejected; only then is
`realpath` allowed, and its result must equal the expected canonical path.
Absolute user input, `..`, symlink components, hard-linked regular files,
alternate worktrees and external resolution fail closed.

Commands use an empty inherited environment, isolated `HOME`/`TMPDIR`, disabled
global/system Git config and only the exact already-installed Node/npm/
TypeScript/Git/Swift/Xcode/shell/system-library roots required to compile and
test. They may not install, update, contact a registry or fall back to a user
cache. Generic search/hash commands may target only literal repository-relative
allowlist roots, never home, Documents, parent, general temp or filesystem root.

The real Codex state/install roots, real Keychains, Docker state and server
configuration are explicit denied roots in Construction.

## 8. PostgreSQL physical-adapter construction

`scripts/r4-gate-b-core-postgres.mjs` exposes a closed typed plan and injected
executor. Construction passes only a fake executor. It accepts no arbitrary
Docker binary, command, SQL path, image, name, environment or cwd.

The future Execution plan remains pinned to:

```text
image tag:      postgres:16.10-bookworm
OCI index:      sha256:38471f330eb885e04de130b768d6db4e10469e2311879c7e5c699f6d2d8a1c74
arm64 manifest: sha256:a64c3894964de33920f4de7cc1e88dfdfa3284bcb0c7c760a0512680a34a8faf
runtime image:  postgres@sha256:38471f330eb885e04de130b768d6db4e10469e2311879c7e5c699f6d2d8a1c74
platform:       linux/arm64
database:       forme_r4_gate_b
network:        none
host ports:     zero
```

The mutable tag is a human label only and may never appear as the image
argument of a future runtime command. The runner uses the exact content-
addressed reference with `--platform linux/arm64` and verifies that its
resolved arm64 manifest is the pinned value before container creation.

Exact container, volume and run-root names become run-ID scoped in the later
Execution Manifest. SQL travels only on `psql -X --set ON_ERROR_STOP=1` stdin,
never argv or evidence. Concurrent workers use exact process groups and are
reaped before cleanup.

The body-free marker is fsynced with creation intent before a volume,
container or worker is started, then atomically records exact name/ID/label and
ownership state after each effect. Faults in every before/after marker window
must still converge to cleanup. Future cleanup removes the container, volume,
workers, synthetic database and run root in Green/Yellow/Red. Under approved
D5, the exact public content-addressed PostgreSQL image cache may remain and is
disclosed; it contains no synthetic database bytes and is not claimed as
runner-owned.

## 9. Codex zero-call diagnostic construction

The Full `forme-fresh-response.sb` remains immutable. Core creates a separate
`forme-codex-zero-call.sb` used only for these future commands:

```text
codex --version
codex app-server --help
codex app-server generate-json-schema --out ${SCHEMA_ROOT}
codex app-server --listen stdio://
```

### 9.1 Canonical roots

```text
${RUN_ROOT}/install/FormeLocal.app/Contents/Resources/Codex/codex
${RUN_ROOT}/install/FormeLocal.app/Contents/Resources/forme-codex-zero-call.sb
${RUN_ROOT}/build/codex-app-server-schema
${RUN_ROOT}/tmp/codex-neutral-cwd
${RUN_ROOT}/auth/home
${RUN_ROOT}/auth/codex-home
${RUN_ROOT}/tmp/codex
${RUN_ROOT}/staging-evidence/codex-zero-call.json
```

The supervisor sets `umask 077`. Directories are `0700`, staged executable
`0500`, staged/generated non-executable files `0600`; schema root begins empty
and neutral cwd contains no `.git`. These
roots replace the conflicting Node/Swift aliases without changing the frozen
Full contract.

### 9.2 No-search staging

Future Execution receives one exact absolute Codex package root and may open
only:

```text
bin/codex.js
vendor/aarch64-apple-darwin/codex/codex
```

The package root and every intermediate path component must be canonical,
current-user-owned and non-symlink. Both source files must be regular,
single-link files opened no-follow. The JS launcher must hash to
`sha256:134063e133f0b4244fa3b251acf973d4fe4b4aeeacbdc135211bf480f59f1477`;
the native binary to
`sha256:1da3f4e0e96028b8a771814293c3033dafd1971f943f6c7e79b0897fe705f590`.
The JS file is evidence only and never executed.

Native bytes are copied from the already-open source fd into an exclusive,
no-follow destination; a pre-existing destination rejects before deletion or
overwrite. The adapter `fstat`s the same source fd again after copy and rejects
identity/size/time/mode drift, fsyncs destination and parent, applies `0500`,
then re-lstats and re-hashes the stage. The repository profile must itself be
a canonical tracked regular, current-user-owned, single-link file. It is
copied from an already-open fd with the same source/stage checks and must match
the later Manifest-pinned repository and staged hashes. No search, glob,
package discovery, PATH lookup or fallback exists.

### 9.3 Diagnostic Seatbelt and process supervisor

The deny-by-default profile has this complete filesystem allowance:

```text
read-only
  exact staged Codex executable and staged profile
  /System/Library
  /usr/lib
  /private/var/db/dyld
  /dev/null
  /dev/random
  /dev/urandom

read + write
  exact schema root
  exact isolated HOME
  exact isolated CODEX_HOME
  exact isolated TMPDIR

read-metadata only
  exact neutral cwd
```

Every other literal or subpath is denied, including network/DNS/loopback/Unix
sockets/listeners, Workspace, repository, connector/candidate roots and real
home/Codex home. If the pinned host needs another runtime root or device, the
lane returns Yellow and requires a new Manifest; it may not widen at runtime.
The one initial transition to the literal staged Codex executable is allowed;
descendant exec/fork is denied. If that distinction cannot be proved on the
pinned host, the lane returns controlled Yellow without widening.

The closed argv prefix, in this exact order, is:

```text
/usr/bin/sandbox-exec
-f ${STAGED_PROFILE}
-D STAGED_CODEX=${STAGED_CODEX}
-D STAGED_PROFILE=${STAGED_PROFILE}
-D SCHEMA_ROOT=${SCHEMA_ROOT}
-D ISOLATED_HOME=${HOME}
-D ISOLATED_CODEX_HOME=${CODEX_HOME}
-D ISOLATED_TMPDIR=${TMPDIR}
-D NEUTRAL_CWD=${NEUTRAL_CWD}
--
```

It is followed by exactly one of:

```text
${STAGED_CODEX} --version
${STAGED_CODEX} app-server --help
${STAGED_CODEX} app-server generate-json-schema --out ${SCHEMA_ROOT}
${STAGED_CODEX} app-server --listen stdio://
```

Those are the logical future argv and always retain literal
`/usr/bin/sandbox-exec` at index zero. Construction connects only an injected
`CodexSpawnPort`: it first validates the complete logical argv, six-variable
environment and cwd, then at the final spawn boundary replaces index zero with
one construction-pinned canonical fake-wrapper path beneath the temp root.
That path is not caller input; every other argv element is unchanged. The fake
wrapper `exec`-replaces itself with the pinned fake Codex. The production spawn
port is not constructible without a separately approved Execution Grant. Each
fixture invocation is still capped at wrapper/fake-Codex one process, with one
additional descendant only in the named adversarial case; all process groups
must be reaped before return.

No shell is used. The working directory is exactly `${NEUTRAL_CWD}`. The
controller supplies an `env -i` equivalent with exactly these six variables
and inherits no others:

```text
HOME=${HOME}
CODEX_HOME=${CODEX_HOME}
TMPDIR=${TMPDIR}
PATH=/usr/bin:/bin:/usr/sbin:/sbin
NO_COLOR=1
CODEX_DISABLE_ANALYTICS=1
```

Each invocation has a fresh process group and these ceilings:

| Step | Deadline | Stdout ceiling |
|---|---:|---:|
| version | 5 s | 256 bytes |
| help | 5 s | 1 MiB |
| schema generation | 30 s | 1 MiB |
| initialize response | 10 s | 4 MiB aggregate; 1 MiB/line |

All four exit codes are exactly zero and all stderr sizes are exactly zero.
Version stdout is exactly `codex-cli 0.145.0\n`; schema-generation stdout is
zero bytes. Help stdout is bounded and hashed but not approved by value.
After handshake stdin-close, exit latency is at most 2 seconds. Success and
every controlled error path close pipes, signal the exact process group with
TERM, wait at most 2 seconds, KILL survivors, reap the leader and prove the
group absent before returning. There is no parent-death watchdog: SIGKILL of
the runner, Desktop Agent/OS crash or power loss is not claimed to auto-reap.
An unknown active group after parent death is Yellow/quarantine.

### 9.4 Schema, wire, writes and evidence

Node filesystem APIs enumerate only the exact schema root. Canonical names are
UTF-8 POSIX paths beginning `./`; NUL, control/newline, backslash, absolute,
empty, `.` and `..` segments are rejected. Only directories that are required
ancestors of accepted files may exist, each at mode `0700`; an empty or extra
directory rejects. Every entry is a regular single-link file. The aggregate
algorithm is exactly:

```text
recursively enumerate regular files below the exact schema root
canonical name = "./" + POSIX path relative to schema root
sort canonical names by unsigned raw UTF-8 bytes
for each name: line = lowercase-file-sha256-hex + "  " + name + "\n"
aggregate = SHA-256(concatenated line bytes)
```

There must be exactly 273 files and aggregate
`sha256:313baf8277ad3b5a3efdbfe1388762f0f41305ef0ea60c3e170c6bc28ec00a62`.
These six literal names and hashes are also required:

```text
./codex_app_server_protocol.schemas.json     sha256:b6ec47c51bef8a6857dd9435f9919a43ba6616a5a9e3ab25087451cbc93d9f06
./codex_app_server_protocol.v2.schemas.json  sha256:bf840d4cb1f74ed64badfaba37bd44999f1ccf6c721f03c2d196ad7f0991dab0
./ClientRequest.json                         sha256:cc9f6e191a032bdfdc96d768f4ddaba4ced75408017af3ac0dbcc4d00c1faaa8
./ClientNotification.json                    sha256:a30b3041578845b11add3d07d5a63cd3a12d5d126e87b8c591862b4aeb68d97c
./ServerRequest.json                         sha256:177f8b7de9dd8ce2055c56a5fd22dbc6ec24e2a4d706d08fef9f5ad4effe71f6
./ServerNotification.json                    sha256:7f9f9ac791c067e3e9840a9a6543a3a0d3de86e64b2d764ede4c2f3b01bcba64
```

`selectedSchemaSetSha256` is SHA-256 of those six files' ordinary aggregate
lines, sorted by the same raw UTF-8 filename rule.

Only after aggregate verification, pinned `ajv@8.20.0` and
`ajv-formats@3.0.1` register those local schemas and local references. The
target's literal `$schema` selects one of exactly two closed constructors:
`http://json-schema.org/draft-07/schema#` uses the default `Ajv` class;
`https://json-schema.org/draft/2020-12/schema` uses `ajv/dist/2020.js`
`Ajv2020`. Missing, alternate or inconsistent dialect is
Yellow/no-handshake and requires a new Manifest; there is no heuristic
fallback. Both constructors use exact options `strict=true`,
`strictSchema=true`, `allErrors=false`, `coerceTypes=false`,
`removeAdditional=false`, `useDefaults=false`, `allowUnionTypes=false`,
`validateFormats=true`, `unicodeRegExp=true`, `ownProperties=true`, and no
custom keyword or async loader. `ajv-formats` uses `mode="full"` and
`keywords=false`. Duplicate `$id` rejects. Remote `$ref`, network loader and external
schema resolution are disabled. The initialize result validator is selected
only by literal canonical filename `./InitializeResponse.json`; no glob,
substring or runtime fallback is allowed. That file and all its transitive
references must belong to the verified 273-file set. A missing/ambiguous file,
unresolved local reference or invalid result fails before `initialized` is
written. The Construction fixture is named `synthetic-real-shape`; it is not
claimed as a response captured from real Codex.

After the complete aggregate passes, the target's own `$schema` must be a
string equal to one approved URI above. Only canonical local `$ref` edges are
traversed. A referenced schema with no `$schema` inherits that one selected
dialect; an explicit `$schema` must equal it byte-for-byte. Alternate URI,
cross-dialect edge, path escape, duplicate `$id` or unresolved ref returns
Yellow before stdio app-server spawn or stdin write. Exactly one constructor
is instantiated; both dialects are never registered together and there is no
retry with the other constructor.

The only client writes are the frozen `initialize` request with id `0` and,
after one valid result, the frozen `initialized` notification. An attempted
forbidden client method rejects before any stdin write. Any server request
causes an immediate stop, with no response and no later client write. The
stream must contain exactly one response with `id = 0`, no duplicate response
and no partial trailing buffer when it ends. The only optional notification is
one `remoteControl/status/changed` with exact `disabled` state. Its raw machine
and installation fields may exist only in bounded validation memory; they are
stripped before hashing and never enter an exception, log or evidence.

The exact allowed top-level isolated `CODEX_HOME` set is:

```text
.tmp
goals_1.sqlite
installation_id
logs_2.sqlite
memories_1.sqlite
skills
state_5.sqlite
state_5.sqlite-shm
state_5.sqlite-wal
tmp
```

A session/rollout file, unknown name, symlink/hard-link, body or auth material
is Red. Owned `Buffer`/`Uint8Array` raw help/wire and identity bytes are
overwritten after validation and digesting. Parsed V8 strings/objects may have
runtime-managed copies: they have bounded references/lifetime and enter no
file, evidence, log or exception, but byte-for-byte V8/GC memory zeroization is
not claimed.

The observed write inventory uses only root tags `schema`, `home`,
`codex_home`, `tmp` and POSIX relative names. Symlinks/hard-links reject.
Entries sort by root-tag bytes then relative-name UTF-8 bytes. Each aggregate
line is exactly:

```text
root-tag + "\t" + relative-name + "\t" + file|directory + "\t"
  + four-digit-octal-mode + "\t" + size-token + "\t"
  + hash-token + "\n"
```

For public `schema` regular files, size-token is exact decimal size and
hash-token is their approved content SHA-256; schema directories use `0` and
`-`. For `home`, `codex_home` and `tmp`, both tokens are literal `-` for every
entry: names/type/mode are aggregated, but machine-specific size/content is
not. Private-root raw bytes and any transient digest used for forbidden-
content checks are never persisted and their owned byte buffers are
overwritten. SHA-256 of the lines is the write-inventory aggregate. Only that
aggregate—not lines or private content commitments—is persisted.

`schemas/r4/gate-b-core/evidence.schema.json` requires this exact Codex object;
every key is required, nullable fields are explicit, and
`additionalProperties` is `false` at every level:

```text
schemaVersion: const r4.gate-b-core.codex-zero-call-evidence.v1
status: ZERO_CALL_CHILD_MECHANISM_GREEN | YELLOW | RED
reasonCode: closed schema enum
profileSourceSha256: sha256
profileStagedSha256: sha256
launcherSourceSha256: sha256
nativeSourceSha256: sha256
nativeStagedSha256: sha256
logicalCommandShapeSha256: {version:sha256,help:sha256,schema:sha256,initialize:sha256}
reportedVersion: "codex-cli 0.145.0" | null
helpStdoutSha256: sha256 | null
helpStdoutBytes: nonnegative integer
schemaStdoutBytes: nonnegative integer
schemaFileCount: integer 0..273
schemaAggregateSha256: pinned sha256 | null
selectedSchemaSetSha256: sha256 | null
initializeResultSha256: sha256 | null
sanitizedNotificationSha256: sha256 | null
clientWrites: integer 0..2
serverResponses: integer 0..1
serverRequests: const 0
serverNotifications: integer 0..1
exitCodes: array of 0..4 integers
stderrBytes: nonnegative integer
isolatedWriteInventorySha256: sha256 | null
processGroupsStarted: integer 0..4
syntheticDescendantsStarted: integer 0..1
allStartedGroupsReaped: boolean
allStartedGroupsAbsent: boolean
threadStarts: const 0
turnStarts: const 0
providerCalls: const 0
providerBytes: const 0
networkBytes: const 0
cleanupPassed: boolean
aiLaneEnabled: const false
aggregateVerdict: YELLOW | RED
```

`initializeResultSha256` is the canonical hash of only
`{schemaVersion:"r4.codex.initialize-result-sanitized.v1",responseId:0,schemaValid:true}`
after full validation. It is never a hash of the raw result.

The evidence schema also has an exact conditional: when
`status = ZERO_CALL_CHILD_MECHANISM_GREEN`, `reportedVersion` is the pinned
string, help/schema/selected/initialize/write hashes are non-null,
`schemaFileCount = 273`, `schemaAggregateSha256` is pinned,
`clientWrites = 2`, `serverResponses = 1`, `serverRequests = 0`,
`serverNotifications` is `0` or `1`, `exitCodes = [0,0,0,0]`,
`schemaStdoutBytes = 0`, `stderrBytes = 0`, `processGroupsStarted = 4`,
`syntheticDescendantsStarted = 0`, both group booleans and `cleanupPassed` are
true, every thread/turn/provider/network counter is zero,
`aiLaneEnabled = false`, and `aggregateVerdict = YELLOW`. Yellow/Red may have
explicit null/partial fields but can never report a Green aggregate.

Hashes may describe approved public tool/profile/schema bytes and sanitized
objects only. Evidence contains no argv, raw output, environment, path, body,
credential, raw or hashed machine/install identity, prompt, transcript or
source.

Classification is exact:

| Observation | Result |
|---|---|
| clean mechanism | `ZERO_CALL_CHILD_MECHANISM_GREEN`; AI lane and aggregate Retry remain Yellow/manual-only |
| host/tool/profile unsupported, initial-exec distinction unprovable, or cleanly reaped timeout | Yellow/manual-only |
| source/profile/schema/method hash drift, wire or write escape, actual network/boundary bypass, sensitive residue, wrong-process risk, or cleanup failure | Red |

It proves no model/provider call, `ResponseTransportGateV1`, Fresh Session
tool inventory, source visibility, candidate or publication authority.

## 10. Transient Candidate macOS Core

The Core candidate lifecycle is exactly:

```text
created
→ fixed 32-KiB ingress region allocated and successfully mlocked
→ inherited frame read directly into the locked region
→ oversize/trailing input rejected
→ frozen candidate schema/hash, exact binding and deadline validated
→ one device-owner presence ceremony
→ noneditable one-shot review displayed
→ approve_exact | discard | authority_expired | controlled_failure
→ binding/hash/deadline revalidated
→ one in-helper counting discard | zero handoff
→ every owned buffer zeroized on controlled terminal
→ cleanup committed
```

The input is one canonical UTF-8 JSON frame of at most 32 KiB with no trailing
bytes and this exact top-level wrapper:

```text
schemaVersion = transient_candidate_frame.v1
candidate = exact frozen ResponseCandidateV1
reservationId
sessionEnvelopeHash
startAuthorizationHash
```

The nested candidate first passes the frozen `validateResponseCandidateV1`
contract. Its `candidateHash` is the canonical hash of the entire candidate
with only `candidateHash` omitted; it is not a response-text hash. The nested
`roomId`, `projectionId`, `interactionId`, `sessionEnvelopeId`, `candidateId`,
`twinBasisHash`, `snapshotManifestHash`, `sessionReceiptHash`, `policyHash` and
`sourceDisclosureClass`, plus the wrapper's reservation/start/session hashes,
must equal the closed synthetic run/session contract. The source class is
exactly `fresh_native_sanitized_snapshot_owner_reviewed`, so Session Envelope
and snapshot hashes are non-null. The effective authority deadline is the
earliest of candidate `expiresAt`, Session Envelope `authorityExpiresAt`,
Interaction expiry and helper start plus 15 minutes; it is checked before
presence and immediately before approval and is never extended. Duplicate
frames, trailing bytes or one mismatch deny before presence. Core supports
only approve-the-exact-displayed-candidate or discard. Editing/replacement,
later review, process/restart recovery, automatic retry and provider
replacement are unavailable.

Construction and the future zero-provider Retry use an injected, in-helper
`CountingHandoffPort`. On approve, the helper rechecks candidate hash, every binding
and authority deadline, immediately discards the synthetic candidate bytes,
and exposes only `handoffCount = 1` plus schema-valid/body-free booleans. No
body-bearing fd, pipe, stdout/stderr field or Desktop Agent value leaves the
signed helper. Every other terminal exposes count zero. A real typed connector
or publication handoff is unavailable and requires separate construction plus
the First Provider-Call Test Grant; PostgreSQL fixture Response delivery does
not pretend that bridge already exists.

### 10.1 Honest memory, UI and crash claim

Forme intentionally persists zero candidate-body bytes. Every Forme-owned
candidate-body buffer is fixed-size, bounded and successfully `mlock`ed before
the first candidate byte is read. An `mlock` failure is Yellow/manual-only with
zero read, display and handoff. Parsing uses offsets/views into the locked
ingress region; canonicalization uses a separately preallocated and locked
scratch region. It may not materialize `responseText` into an untracked Swift
`String` or Foundation JSON object. The UI bridge uses the locked bytes with a
no-copy lifecycle; framework-internal copies remain the explicit caveat below.
Every explicit Forme-owned ingress, canonicalization and UI-bridge buffer is
bounded and zeroized on controlled normal/cancel/expiry/failure terminals. The
noneditable review validates the frozen candidate hash before display and
rechecks immediately before approve; any change produces zero handoff.

The review window disables pasteboard/copy, drag/drop, printing, Services,
state restoration, listeners and external assets. It neither keeps history nor
offers edit/replacement. AppKit may create lifecycle-bounded framework-managed
process-memory copies; universal framework/swap/crash-diagnostic erasure is not
claimed. Unexpected process death proves only that the process ended and the
transient candidate is unrecoverable; it does not claim byte-for-byte crash
zeroization.

The no-disk proof covers exact Forme-owned run roots and synthetic canaries. It
does not claim protection from swap, privileged OS diagnostics, root/debugger,
accessibility/screen-recording authority, physical observation or arbitrary
non-Forme system storage.

### 10.2 Exact user-presence ceremony

Production code uses a fresh `LAContext` for every ceremony, sets
`touchIDAuthenticationAllowableReuseDuration = 0`, calls
`canEvaluatePolicy(.deviceOwnerAuthentication, error:)`, then exactly one
`evaluatePolicy(.deviceOwnerAuthentication, localizedReason:)` with:

```text
Approve this exact Forme demo response for one synthetic handoff.
```

The context is invalidated on every terminal. There is no biometrics-only
policy, reused context, cached-success fallback or alternate authorization.
Cancelled, unavailable, expired or ambiguous presence returns Yellow/manual-
only, produces handoff count zero and is acceptable only with Green cleanup.
NSError text, biometric type and user/device detail never enter evidence.
Construction uses an injected fake presence port only.

### 10.3 Candidate Keychain prohibition and temporary signing scope

Core creates no candidate key and performs zero candidate Secure Enclave,
login Keychain or Data Protection Keychain operation. A future custom temporary
file Keychain is limited to one-run self-signed test signing identity and one
synthetic Room-binding canary. It contains no candidate, candidate key, real
credential, production secret or Guest byte.

The custom-file Keychain mechanism is explicitly an MVP probe with known
support/deprecation uncertainty. Unsupported creation/import/signing is
Yellow with zero helper launch, no fallback to login or Data Protection
Keychain, mandatory cleanup and stop. Any default/search-list mutation or
residue is Red. Future evidence separates reads from mutations:

```text
candidateSecureEnclaveOperations = 0
candidateKeychainOperations = 0
loginKeychainMutations = 0
dataProtectionKeychainMutations = 0
defaultKeychainMetadataReads = exact nonnegative count frozen by Execution Manifest
keychainSearchListMetadataReads = exact nonnegative count frozen by Execution Manifest
customTemporaryKeychainOperations = exact nonzero count frozen by Execution Manifest
realRoomCredentials = 0
```

### 10.4 Separate signed helper and closed effect plan

Construction may compile unsigned Swift but may not assemble/sign/launch an
app. `Package.swift` adds a separate `FormeCoreLocal` executable target. That
target does not link `FormeLocal/Launcher.swift`, `GateBProbe.swift`,
`KeychainProtector.swift` or the Full candidate store. Its bundle accepts
exactly one command, `--gate-b-core-transient-probe`. Missing, help, legacy
`--gate-b-probe`, alternate or additional arguments reject before Security,
LocalAuthentication, UI or candidate reads.

The bundle identifier is `org.chaostudio.forme.gate-b.core-local`, minimum OS
is macOS `26.0`, `LSUIElement = true`, executable is `FormeCoreLocal`, and the
bundle contains only the exact Info.plist, executable and pinned Core profile
named by `build-recipe.json`. `FormeCoreLocal.entitlements` is an exact empty
property-list dictionary. Empty entitlements do not prove kernel network
denial or an access group. The helper constructs no network client/listener;
external evidence may claim only zero observed helper sockets, not universal
OS-enforced network impossibility.

The fake executor constructs this future plan:

```text
compile
→ assemble exact FormeCoreLocal.app bundle
→ snapshot body-free pre-effect Keychain metadata
→ create temporary custom Keychain
→ create/import exactly one self-signed test identity
→ sign and verify exact bundle
→ launch exact helper with --gate-b-core-transient-probe
→ collect closed body-free evidence
→ cleanup and verify absence
```

There is no generic command, shell, cwd, environment, executable, path search,
provider/model, credential or real-data parameter. Future commands must be
frozen as absolute executable plus exact argv in the next Execution Manifest.
External adapter evidence—not helper self-assertion—verifies bundle inventory,
hardened runtime, forbidden-entitlement absence, approved certificate
fingerprint/designated-requirement hashes, staged-byte hashes, observed socket
count and cleanup. Raw certificate, common name and machine identity are
forbidden. The self-signed identity proves one pinned-host test mechanism only;
it does not prove notarization, Team ID, production identity, access group or
distributable security.

Three profiles remain distinct:

- immutable Full `forme-fresh-response.sb`;
- Core zero-call diagnostic profile used in Retry; and
- Core transient-response profile constructed for a later Provider Test but
  not executed by zero-call Retry.

The transient-response profile denies network, fork/descendant exec, Workspace,
repository, connector and candidate roots, and candidate-file output. It may
read only the exact staged child plus pinned system runtime and may read/write
only exact isolated HOME/CODEX_HOME/TMPDIR; bounded prompt/snapshot input is an
inherited descriptor, not a file path. Its actual execution and any body output
remain unavailable until a separate Provider Packet.

The Full persistent candidate, seven-day retention, later/offline review,
restart/crash reconciliation, candidate replacement, Owner edit, submitted-
unknown body recovery/retry, Data Protection Keychain/access group, production
signing/notarization and Full `FileCandidateStore` activation remain
`UNPROVEN_POST_DEMO`.

## 11. Marker, evidence and cleanup

Before every possible external effect, the adapter fsyncs a body-free intent
phase. The marker may contain only schema version, run ID, Manifest hash,
phase, resource-ownership booleans, process-group IDs, body-free hashes,
controlled terminal code and cleanup state. It may not contain body/candidate
hashes, request/prompt/transcript/source, absolute user path, environment,
password/private key, raw argv/output, LocalAuthentication details or machine
identity.

Construction fake-fault tests cover injected exception windows while the
runner/controller remains alive: intent before resource, resource before
marker update, active worker/child and every cleanup step. The journal may
recover owned resource identifiers only; it never recovers a candidate.
Future Retry may additionally terminate its exact synthetic helper/child
abruptly while the parent supervisor stays alive. SIGKILL of the parent,
Desktop Agent/OS crash, kernel panic and power loss are not simulated-crash
proof. Future cleanup order is:

1. commit body-free `cleanup_required`;
2. close pipes, TERM/KILL/reap exact process groups and prove no descendant;
3. terminate helper and discard transient candidate without recovery;
4. remove synthetic Room binding;
5. remove signing identity, then lock/delete custom Keychain;
6. remove certificate/private-key/PKCS#12, app, Codex stage, schema, isolated
   HOME/CODEX_HOME/TMPDIR and build/runtime bytes;
7. remove PostgreSQL container, volume and workers;
8. verify default Keychain/search-list preflight hash, no process/socket/
   Keychain item/container/volume/run-root canary;
9. copy only the body-free summary; fsync and remove the run root.

Cleanup is mandatory and idempotent for Green, Yellow, Red, cancel, timeout,
injected fault and controlled abrupt child termination while the parent lives.
Cleanup uncertainty cannot be Green; a sensitive canary, wrong-process risk,
external path effect, unexpected Keychain mutation, signature drift or owned-
resource residue is Red. If the runner has already reported group absence as
unknown, the controlling Agent may run only Manifest-pinned cleanup and then
stop at the Owner gate; it may not call `ps`, `find`, `rg`, Codex or an ad-hoc
diagnostic.

Final evidence contains only approved hashes/counts/booleans, command-shape
hashes rather than argv, sanitized reason codes and cleanup receipts. It never
contains raw SQL/output/schema, absolute paths, environment, identity,
candidate/request/response, credential, auth, prompt, transcript or source.
The macOS portion additionally requires:

```text
candidateBodyFilesCreated = 0
candidateBodyStdoutBytes = 0
candidateBodyStderrBytes = 0
bodyBearingHandoffOutsideHelper = 0
controlledZeroizationPassed = boolean
crashZeroizationClaimed = false
persistentCandidateRecoverySupported = false
```

## 12. Required offline Construction tests

### 12.1 Cross-surface and PostgreSQL

1. Core operation bijection and exact `32 / 26 / 6 / 21` counts.
2. All 13 excluded method/path pairs return identical pre-parse `404`.
3. Core HTTP/SQL/CLI parity; Web contains no excluded link/form/control.
4. SQL inventory: 37 tables, 44 domains, 3 composites, 11 views, 39
   functions and zero unavailable stub.
5. Runtime roles have zero raw-table grant and only Core EXECUTE grants.
6. Setup function is migrate-only, has no route and binds exact basis hash.
7. Fake Docker plan fixes digest/platform/network/ports/stdin SQL and rejects
   arbitrary command/name/path/environment.
8. Marker injected-fault windows and active fake race-worker cleanup are
   idempotent while the controller remains alive.
9. SQL fixtures cover success, controlled failure, same-key replay,
   same-key/different-hash and terminal read denial.
10. All 13 later physical race plans have exact barriers, actors, expected
    winner/loser results and cleanup; event-sequence/ACK semantics pass the
    separate non-race matrix.

### 12.2 Codex diagnostic

1. exact source paths/hash/mode/owner and fd-based source/staged equality;
2. symlink, hard link, intermediate replacement and pre-existing destination
   denial;
3. exact four sandbox argv/profile parameter sets, exact six-variable
   environment and arbitrary input denial;
4. canonical roots, exact allowlist and deterministic recursive 273-file
   aggregate/selected hashes;
5. missing/extra/nested/symlink/hard-link schema rejection;
6. generated `./InitializeResponse.json`-backed synthetic-real-shape result;
7. every forbidden client/server method denied before write;
8. optional notification strip/hash plus duplicate/unknown rejection;
9. stderr, malformed JSON, nonzero exit, timeout and huge-output failure;
10. the single permitted fake-wrapper/child process shape and one-descendant
    fork/orphan case terminate, reap and prove no survivor;
11. exact allowed/unknown CODEX_HOME write set and cleanup after every result;
12. exit/output/trailing-buffer and body-free evidence validation plus aggregate
    `YELLOW / aiLaneEnabled=false` continuation.

Construction reads or invokes neither real Codex nor `/usr/bin/sandbox-exec`.

### 12.3 Transient macOS Core

1. fake executor performs zero process/network/Security/LA effect and the
   in-helper `CountingHandoffPort` exposes no body-bearing descriptor;
2. exact effect order/argv plan with no shell/generic spawn;
3. bundle extra member, symlink, hard link, mode/hash drift rejection;
4. zero or two signing identities reject; exactly one may continue in fake;
5. signature/entitlement/designated-requirement mismatch rejects;
6. one canonical, exact-field, exact-bound, at-most-32-KiB candidate frame only;
7. duplicate/trailing/late/wrong-hash/wrong-binding candidate rejection before
   presence;
8. fresh-context `.deviceOwnerAuthentication` approve/cancel/unavailable/
   expired transitions, zero reuse duration and exact localized reason;
9. zero pre-approval count, exactly one approve-exact count/discard, zero count
   after every other terminal, with hash/binding/deadline recheck;
10. `mlock` succeeds before the first candidate byte read; ingress,
    canonicalization and UI-bridge buffer zeroizer spies fire on every
    controlled normal/cancel/timeout/schema-fail/abrupt provider-child terminal
    while the helper remains alive; helper-death tests assert unrecoverability
    and never claim crash zeroization;
11. pasteboard, drag/drop, printing, services, restoration, listener and
    external assets disabled;
12. separate Core target rejects every command except
    `--gate-b-core-transient-probe`, does not link the Full launcher/probe/
    Keychain protector and makes zero candidate Keychain/Secure Enclave call;
13. journal intent-before-effect and body-free schema at every fault point;
14. cleanup-always/idempotency and no Green on cleanup failure;
15. synthetic candidate canary absent from every allowed Forme-owned path and
    final evidence; and
16. Keychain metadata-read/mutation counters and unsupported-custom-Keychain
    Yellow/no-fallback behavior are exact; and
17. Core mechanism status never changes the Full persistent lane's status.

### 12.4 Aggregate regressions and effect audit

- all current Gate A and historical Full tests continue to pass as regression,
  but are never renamed as Core physical proof; no existing Full assertion may
  be deleted, renamed, skipped, loosened or reclassified;
- `package-lock.json` is byte-identical and all dependencies unchanged;
- no file outside Section 6 changes and no generated/build/temp byte is
  committed;
- static audit records real Codex/sandbox-exec/Docker/PostgreSQL/signing/
  Keychain/LocalAuthentication/Secure Enclave/provider/network effects at zero;
- all synthetic canaries are absent from reports and tracked output; and
- Construction cleanup leaves no temp root or fake child.

## 13. Future Execution effects requiring a new grant

Construction must return a new exact Execution Manifest. It may propose, but
does not authorize, one zero-provider local Retry with synthetic data.

The future PostgreSQL lane may create one run-ID-scoped container and volume,
execute the exact Core migrations/fixtures/races/rollback and remove all
runtime resources. The pinned public image cache may remain under D5.

The future Codex lane may perform exactly:

```text
external Codex package source opens 2
staged regular files                 2
Seatbelt-wrapped Codex invocations   4
generated schema files               273
client JSONL writes                  2
server requests                       0
thread starts                        0
turn starts                          0
provider calls / bytes / spend       0 / 0 / US$0
Guest/source body bytes              0
real Codex-home reads                 0
external network bytes                0
persisted machine identities          0
```

The future macOS lane may build/sign/launch one helper, create one temporary
test-signing Keychain/identity and one synthetic binding canary, request one
device-owner presence ceremony using a synthetic candidate, count/discard one
approved in-helper candidate, then clean every owned resource. It performs zero
candidate-key/Secure-Enclave operation, exports zero candidate body and uses no
real Guest or Room credential.

All exact commands, tool/profile/file hashes, resource names, observed
write-set limits and machine-specific facts must be frozen in that later
Manifest. The controlling Desktop Agent remains the Owner-approved procedural
boundary: it may invoke only the pinned runner and, on failure, pinned cleanup,
then stop. It may not investigate the host. Kernel isolation of that Agent
itself is not claimed.

## 14. Construction verdict and Owner return gate

Construction returns one of:

- **CORE_CONSTRUCTED_OFFLINE:** exact workset, schemas, SQL, fake physical
  adapters, tests, static effect audit and cleanup all pass; a separate
  Execution Manifest may be reviewed;
- **YELLOW:** repository bytes are reviewable but a future physical boundary
  is unsupported/ambiguous; no Execution approval is recommended; or
- **RED:** any scope, external-read, real-runtime, network, dependency,
  sensitive-evidence, hidden-fallback, test or cleanup violation; all later
  non-cleanup work stops.

The Agent then returns:

1. proposal-containing baseline commit/tree and exact changed-path index;
2. body-free Construction Report and machine evidence hashes;
3. test counts, workset/dependency proof and cleanup receipt;
4. exact Core artifact aggregate and immutable-Full aggregate;
5. the new Execution Manifest and low-load review hashes;
6. a plain-language account of every future local effect and remaining claim
   gap; and
7. a recommendation on whether the Owner should approve one Retry.

It then stops. `CORE_CONSTRUCTED_OFFLINE` never starts Docker, PostgreSQL,
Codex, `sandbox-exec`, signing, Keychain, user presence or provider. Retry
Execution, First Provider Call, Gate C, deployment, public traffic, merge and
spend remain separate future Owner decisions.
