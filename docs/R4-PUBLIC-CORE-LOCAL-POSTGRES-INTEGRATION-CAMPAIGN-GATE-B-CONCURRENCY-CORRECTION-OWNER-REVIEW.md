# R4 #67 Integration Campaign Gate-B Concurrency Validation Correction — Owner Review

- Status: **`OWNER_APPROVAL_REQUIRED`**
- Date: 2026-08-15
- Construction effect: repository only
- Integration Campaign / Docker / cleanup / PostgreSQL / SQL:
  **NOT_AUTHORIZED**
- Production: **NOT_REQUESTED**
- Gate C: **NOT_REQUESTED**

## What you are deciding

The approved inspect-missing implementation `Kic` is committed and its own
functional suites are Green. The remaining construction cannot honestly freeze
evidence because the default concurrent Gate-B Core regression command failed
twice at the same pre-existing synthetic macOS feeder terminal check. That
focused test and the complete serial Gate-B set are Green, so the discrepancy
is a load-sensitive validation ambiguity rather than an inspect-parser failure.

This Review asks for one medium-grained repository correction and completion
package. Approval lets the existing Gate-B feeder terminal wait use the
remaining already-bound absolute authority deadline, adds deterministic
positive and fail-closed tests, and then completes the original strict evidence
and status freeze. It does not waive concurrent validation and does not
authorize another Integration Campaign or any external effect.

The complete proposal is the
[Gate-B Concurrency Validation Correction Addendum](./R4-PUBLIC-CORE-LOCAL-POSTGRES-INTEGRATION-CAMPAIGN-GATE-B-CONCURRENCY-CORRECTION-ADDENDUM.md).

## Exact proposal and predecessor binding

| Binding | Exact value |
|---|---|
| correction Addendum SHA | `sha256:f18dce72f2a53ce11e530499156852e815c132093962ab7bdd5cee6dec587ae7` |
| Addendum HEAD / tree | `e21c441a6154f3c08b96ccbf7f0f7b693107b99a` / `8860d06a9ccc821be62bf62ee79cd356b8b7fb52` |
| Addendum parent `Kic` HEAD / tree | `1e93280bc3842d40e6f797a38ca4bfa2a2277813` / `14526ed95feae2da6c9c14a89ece48616822e4e8` |
| Addendum delta | exactly one added mode-`100644` document |
| approved inspect Addendum SHA / HEAD | `sha256:a43fbb5ac8e795ab6b6bf51507d3489c9e803161419af50179d7f7e9bed97505` / `15536817efe6b0b7a5c8014e912f02429f197ef1` |
| approved inspect Review SHA / HEAD | `sha256:ed73deb10f3ec9dcd5501f6d25f40ec7b140838494521ddd4cc9574b95afe324` / `7e07672563201d92db14ecf3adcbf5f6ad970b9f` |
| `Kic` exact delta | two modified paths: local-PostgreSQL runner and focused test |
| `Kic` runner SHA / bytes | `sha256:f012fd876cc49bd14be3519af6aeef98d7451ab29c90c786a7f242f59915984c` / `735755` |
| `Kic` test SHA / bytes | `sha256:25e51e491a37e1e048e6bce590c8cf5d08599a4fa8b2fd02cf83a17523280b83` / `196175` |
| `Kic` aggregate | `sha256:5dea77a0e5391f3283caab8a9b1d67f4a6ed8758be6be3631bad5283ca530f1f` |

The Addendum is a direct, single-parent child of `Kic` and adds only its own
document. It grants no runner, test, dependency, effect authority or current
status claim by itself.

## Nine choices

Please accept or reject these as one decision:

1. keep the exact default concurrent Gate-B Core command mandatory rather than
   substituting its focused or serial form;
2. derive feeder terminal time only from the remaining already-bound absolute
   authority deadline and existing bounded cleanup grace;
3. preserve non-Green handling for missing/error/signal/nonzero/malformed
   terminal and unproven process-group absence;
4. correct the shared runner path, not just test timeout, skip or concurrency
   configuration;
5. add deterministic synthetic within-authority and after-authority coverage
   without a long real sleep or real helper/provider;
6. require two consecutive Green default concurrent Gate-B runs plus one Green
   serial full run;
7. bind immutable `Kic` and new `Kgc` truth in `Lic`, then complete the original
   `Mic` report and nine current-status surfaces;
8. leave both retained campaign roots and all consumed V1 grant/evidence/journal
   bytes untouched; and
9. keep Docker, cleanup, PostgreSQL, production, real Guest, Provider and Gate C
   effects at zero throughout construction.

## Exact construction envelope

Approval authorizes the corrected chain
`Kic → Addendum → Review → Kgc → Lic → Mic`. After this Review, exactly three
direct, single-parent construction commits may be created:

- `Kgc`: modify only
  `scripts/r4-gate-b-physical-runner.mjs` and
  `test/r4-gate-b-core/physical-runner.test.ts`;
- `Lic`: add only the inspect-missing correction artifact index, strict evidence
  schema and machine evidence already named in the Addendum; and
- `Mic`: add only the original inspect-missing construction report and modify
  only `README.md`, `docs/CONTROL.md`, `docs/DECISIONS.md`,
  `docs/NATIVE-HARNESS-ARCHITECTURE.md`, `docs/PRODUCT.md`,
  `docs/R4-PUBLIC-CORE-GATE-C-ACTIVATION-CARD.md`, `docs/README.md`,
  `docs/ROADMAP.md` and `docs/VALIDATION.md`.

Total new construction workset: **15 paths = 11 modified + 4 added in exactly
three commits**. `Kic` remains an immutable predecessor outside this new delta.
No dependency, SQL, production schema, physical authority, mode, retained root
or sixteenth construction path may change.

## Validation and evidence boundary

Approval requires committed-byte lineage and hash verification; deterministic
within/after-authority feeder tests; exact journal/start/release/terminal and
cleanup ordering; two consecutive default concurrent Gate-B Core Green runs;
one serial full Gate-B Green run; and all predecessor focused, offline, spine,
TypeScript, Ajv, docs, inventory, no-server-AI and diff checks Green.

`Lic` may record committed `Kic`/`Kgc` and candidate-`Lic` truth only. `Mic` may
record the external committed-`Lic` audit and candidate-`Mic` truth only. The
post-`Mic` committed audit remains external and may not be written back into
`Mic`. Any ambiguity stops without claiming Technical Review Green.

All tests remain deny-network and synthetic. They may not resolve or call
Docker, open PostgreSQL, execute SQL, start a real helper/provider or read either
retained Integration Campaign root.

## What approval does not do

Approval does not:

- retry, replace, clean or reinterpret the consumed V1 campaign;
- create or consume a V2 grant;
- read the Docker socket or call the Docker CLI/daemon;
- inspect, stop or remove a Docker object;
- pull an image, connect PostgreSQL, run SQL or exercise a domain action;
- activate production, publish, admit a Guest, call a Provider, deploy, release,
  spend or enter Gate C.

## Approval mechanics

An exact approval must bind:

1. the Addendum SHA and Addendum HEAD/tree above;
2. this Review's externally computed committed blob SHA and direct-child Review
   HEAD/tree;
3. all nine choices and the exact corrected topology and 15-path/three-commit
   construction envelope; and
4. the zero-effect boundary and mandatory stop below.

This Review intentionally contains neither its own SHA nor its future commit
HEAD/tree. Those values must be computed from committed bytes and supplied
externally, avoiding self-reference.

## Mandatory stop

After exact approval and successful committed construction/audit:

`LOCAL_POSTGRES_INTEGRATION_CAMPAIGN_INSPECT_MISSING_CORRECTION_TECHNICAL_REVIEW_GREEN / FRESH_INTEGRATION_CAMPAIGN_V2_EXECUTION_APPROVAL_REQUIRED / PRODUCTION_NOT_REQUESTED / GATE_C_NOT_REQUESTED`

Any out-of-envelope path, serial-only waiver, authority-window expansion,
validation ambiguity or requested external effect stops earlier and returns to
the Owner.
