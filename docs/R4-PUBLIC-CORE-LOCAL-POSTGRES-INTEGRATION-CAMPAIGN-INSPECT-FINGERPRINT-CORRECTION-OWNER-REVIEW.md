# R4 Public Core Local PostgreSQL Integration Campaign Inspect-Fingerprint Correction Owner Review

Status: approved inside the Owner-confirmed local MVP outcome envelope

Date: 2026-08-16

## Exact authority reviewed

- Addendum SHA-256:
  `sha256:ef63d738a208c3c2469e8c7f290daaf9ffe2e8166b696239436a3b22abf4bf39`;
- Addendum HEAD/tree:
  `25cf2b400d96a86c8017f7dbc2e2186fff7e9180` /
  `d858b46c9f6e83edd57db22033ce7458952f774f`;
- predecessor Integration Campaign V3 Review HEAD/tree:
  `c0382bedf25afc9464254f0d3b3f461e4a364e12` /
  `1268459f984087419b24db66f2a11fb322eb4f78`.

The Addendum is the only path added by its direct-child commit. This Review is
the only path added by the next direct-child commit. This Review intentionally
does not contain its own future blob hash, HEAD or tree.

## Decisions

1. Accept the V3 terminal evidence as a truthful fail-closed result, not a
   Green rehearsal and not a retryable V3 grant.
2. Accept the exact body-free Docker diagnostic tuple as the complete new
   evidence basis. The stderr body remains unobserved and inadmissible.
3. Permit `MISSING_EXACT` only for the exact plan-bound container command,
   target, exit/signal, UTF-8/line framing, byte counts and SHA-256 pair. Every
   mutation remains unknown/fail-closed.
4. Accept the exact `Kif -> Lif -> Mif` three-commit, fifteen-path repository
   construction and the fresh versioned final Card/Review authority paths.
5. Preserve every prior Docker/PostgreSQL/SQL/write-ahead/no-retry/cleanup
   ceiling. The correction does not authorize an extra pull or any direct
   diagnostic outside the runner.
6. Keep the V3 root immutable forensic history. It must not be reused,
   modified or cleaned by the successor campaign.
7. Correction construction is repository-only and may autonomously include
   implementation, hostile tests, evidence/status freeze, commits and exact
   Card/Review audit inside the confirmed outcome envelope.
8. One final fresh local campaign prepare and the same grant's one-use
   lifecycle may occur only after all committed-byte and offline validation is
   Green and only within the remaining local lifecycle budget.
9. Production, real Guest/private data, Provider/model calls, publication,
   traffic, messaging, deployment, release, spend, foreign-resource cleanup
   and Gate C remain `NOT_REQUESTED`.

## Required validation

The committed implementation must pass the captured positive tuple, all
single-field hostile mutations, the existing literal missing-message paths,
all Integration Campaign tests, the complete R4 runner suite, Gate-B Core,
repository spine, typecheck, room no-AI, docs audit, inventory and diff/syntax
checks. Construction evidence must state zero external/runtime effects.

## Stop

After repository construction and final versioned Card/Review audit, the agent
may use the final remaining authorized local lifecycle. Any mismatch,
unexpected image acquisition, ambiguous cleanup, foreign-resource collision
or exhausted ceiling stops without retry.

Success must stop at:

`LOCAL_POSTGRES_WIRING_TECHNICAL_REVIEW_GREEN / PRODUCTION_NOT_REQUESTED /
GATE_C_NOT_REQUESTED`
